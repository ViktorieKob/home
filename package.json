{
  "name": "home-budget-app",
  "private": true,
  "version": "1.0.0",
  "description": "Spolecny rozpocet Viki a Katy - lokalni dev server",
  "scripts": {
    "dev": "live-server --host=0.0.0.0 --port=5173 --no-browser --quiet",
    "dev:open": "live-server --host=0.0.0.0 --port=5173"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
